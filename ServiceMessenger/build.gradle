apply plugin: 'java'
apply plugin: 'idea'

dependencies {
    compile project(':Framework')
}

jar {
    from {
        configurations.compile.collect {
            it.isDirectory() ? it : zipTree(it);
        }
    }
}